<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eco_shop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecoshop">
        <activity android:name=".user_page.UserPageActivity"></activity>
        <activity android:name=".checkout.ConfirmOrderActivity" />
        <activity android:name=".food_detailed.FoodDetailedActivity" />
        <activity android:name=".menu.MenuActivity" />
        <activity android:name=".sign_up.RegistrationActivity" />
        <activity android:name=".sign_in.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".splash_screen.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>